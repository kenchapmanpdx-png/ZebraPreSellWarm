# Database (Neon Postgres serverless)
# Set in Vercel Project Settings → Environment Variables
# Format: postgresql://user:pass@host/db?sslmode=require
DATABASE_URL=

# When DATABASE_URL is unset, serverless functions still respond 202 (logged
# only). Set this in production to persist waitlist/preorder/contact submissions.
